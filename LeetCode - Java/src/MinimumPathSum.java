import java.util.ArrayList;
import java.util.List;
public class MinimumPathSum {
    //We can Probably use A* algorithm here, first we must create a graph data type.
    private class MinimumPathGraph {
        public List<WeightEdge<Integer>> neighbors(int i, int j, int[][] grid) {
            ArrayList
        }
    }
}
